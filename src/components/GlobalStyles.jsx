import React, { useEffect } from 'react';

const GlobalStyles = () => {
  useEffect(() => {
    // This could also just be imported as a CSS file in main.jsx
    // but the summary asks for GlobalStyles.jsx
  }, []);

  return null;
};

export default GlobalStyles;
